// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: bookmarks.sql

package queries

import (
	"context"

	"github.com/google/uuid"
)

const countBookmarksByUser = `-- name: CountBookmarksByUser :one
SELECT COUNT(*)
FROM document_bookmarks b
JOIN documents d ON d.id = b.document_id
JOIN authors a ON a.id = d.author_id
JOIN categories c ON c.id = d.category_id
WHERE b.user_id = $1
  AND (
    CASE
      WHEN $2::text IS NOT NULL AND $2::text != '' THEN b.kind = $2::text
      ELSE TRUE
    END
  )
  AND (
    CASE
      WHEN $3::text IS NOT NULL AND $3::text != ''
      THEN (
        d.title ILIKE '%' || $3::text || '%'
        OR COALESCE(d.description, '') ILIKE '%' || $3::text || '%'
        OR a.name ILIKE '%' || $3::text || '%'
        OR c.title ILIKE '%' || $3::text || '%'
        OR COALESCE(b.quote_text, '') ILIKE '%' || $3::text || '%'
        OR COALESCE(b.quote_context, '') ILIKE '%' || $3::text || '%'
        OR EXISTS (
          SELECT 1
          FROM document_tags dt
          JOIN tags t ON t.id = dt.tag_id
          WHERE dt.document_id = d.id
            AND t.title ILIKE '%' || $3::text || '%'
        )
      )
      ELSE TRUE
    END
  )
`

type CountBookmarksByUserParams struct {
	UserID uuid.UUID
	Kind   *string
	Search *string
}

// CountBookmarksByUser
//
//	SELECT COUNT(*)
//	FROM document_bookmarks b
//	JOIN documents d ON d.id = b.document_id
//	JOIN authors a ON a.id = d.author_id
//	JOIN categories c ON c.id = d.category_id
//	WHERE b.user_id = $1
//	  AND (
//	    CASE
//	      WHEN $2::text IS NOT NULL AND $2::text != '' THEN b.kind = $2::text
//	      ELSE TRUE
//	    END
//	  )
//	  AND (
//	    CASE
//	      WHEN $3::text IS NOT NULL AND $3::text != ''
//	      THEN (
//	        d.title ILIKE '%' || $3::text || '%'
//	        OR COALESCE(d.description, '') ILIKE '%' || $3::text || '%'
//	        OR a.name ILIKE '%' || $3::text || '%'
//	        OR c.title ILIKE '%' || $3::text || '%'
//	        OR COALESCE(b.quote_text, '') ILIKE '%' || $3::text || '%'
//	        OR COALESCE(b.quote_context, '') ILIKE '%' || $3::text || '%'
//	        OR EXISTS (
//	          SELECT 1
//	          FROM document_tags dt
//	          JOIN tags t ON t.id = dt.tag_id
//	          WHERE dt.document_id = d.id
//	            AND t.title ILIKE '%' || $3::text || '%'
//	        )
//	      )
//	      ELSE TRUE
//	    END
//	  )
func (q *Queries) CountBookmarksByUser(ctx context.Context, arg CountBookmarksByUserParams) (int64, error) {
	row := q.db.QueryRow(ctx, countBookmarksByUser, arg.UserID, arg.Kind, arg.Search)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createBookmark = `-- name: CreateBookmark :one
INSERT INTO document_bookmarks (
    id,
    user_id,
    document_id,
    kind,
    quote_text,
    quote_context,
    page_label
) VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, user_id, document_id, kind, quote_text, quote_context, page_label, created_at, updated_at
`

type CreateBookmarkParams struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	DocumentID   uuid.UUID
	Kind         string
	QuoteText    *string
	QuoteContext *string
	PageLabel    *string
}

// CreateBookmark
//
//	INSERT INTO document_bookmarks (
//	    id,
//	    user_id,
//	    document_id,
//	    kind,
//	    quote_text,
//	    quote_context,
//	    page_label
//	) VALUES ($1, $2, $3, $4, $5, $6, $7)
//	RETURNING id, user_id, document_id, kind, quote_text, quote_context, page_label, created_at, updated_at
func (q *Queries) CreateBookmark(ctx context.Context, arg CreateBookmarkParams) (DocumentBookmark, error) {
	row := q.db.QueryRow(ctx, createBookmark,
		arg.ID,
		arg.UserID,
		arg.DocumentID,
		arg.Kind,
		arg.QuoteText,
		arg.QuoteContext,
		arg.PageLabel,
	)
	var i DocumentBookmark
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DocumentID,
		&i.Kind,
		&i.QuoteText,
		&i.QuoteContext,
		&i.PageLabel,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteBookmark = `-- name: DeleteBookmark :execrows
DELETE FROM document_bookmarks
WHERE id = $1 AND user_id = $2
`

type DeleteBookmarkParams struct {
	ID     uuid.UUID
	UserID uuid.UUID
}

// DeleteBookmark
//
//	DELETE FROM document_bookmarks
//	WHERE id = $1 AND user_id = $2
func (q *Queries) DeleteBookmark(ctx context.Context, arg DeleteBookmarkParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteBookmark, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getBookmarkByIDForUser = `-- name: GetBookmarkByIDForUser :one
SELECT id, user_id, document_id, kind, quote_text, quote_context, page_label, created_at, updated_at
FROM document_bookmarks
WHERE id = $1 AND user_id = $2
`

type GetBookmarkByIDForUserParams struct {
	ID     uuid.UUID
	UserID uuid.UUID
}

// GetBookmarkByIDForUser
//
//	SELECT id, user_id, document_id, kind, quote_text, quote_context, page_label, created_at, updated_at
//	FROM document_bookmarks
//	WHERE id = $1 AND user_id = $2
func (q *Queries) GetBookmarkByIDForUser(ctx context.Context, arg GetBookmarkByIDForUserParams) (DocumentBookmark, error) {
	row := q.db.QueryRow(ctx, getBookmarkByIDForUser, arg.ID, arg.UserID)
	var i DocumentBookmark
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DocumentID,
		&i.Kind,
		&i.QuoteText,
		&i.QuoteContext,
		&i.PageLabel,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getPublicationBookmarkByUserAndDocument = `-- name: GetPublicationBookmarkByUserAndDocument :one
SELECT id, user_id, document_id, kind, quote_text, quote_context, page_label, created_at, updated_at
FROM document_bookmarks
WHERE user_id = $1 AND document_id = $2 AND kind = 'publication'
LIMIT 1
`

type GetPublicationBookmarkByUserAndDocumentParams struct {
	UserID     uuid.UUID
	DocumentID uuid.UUID
}

// GetPublicationBookmarkByUserAndDocument
//
//	SELECT id, user_id, document_id, kind, quote_text, quote_context, page_label, created_at, updated_at
//	FROM document_bookmarks
//	WHERE user_id = $1 AND document_id = $2 AND kind = 'publication'
//	LIMIT 1
func (q *Queries) GetPublicationBookmarkByUserAndDocument(ctx context.Context, arg GetPublicationBookmarkByUserAndDocumentParams) (DocumentBookmark, error) {
	row := q.db.QueryRow(ctx, getPublicationBookmarkByUserAndDocument, arg.UserID, arg.DocumentID)
	var i DocumentBookmark
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DocumentID,
		&i.Kind,
		&i.QuoteText,
		&i.QuoteContext,
		&i.PageLabel,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listBookmarksByUser = `-- name: ListBookmarksByUser :many
SELECT b.id, b.user_id, b.document_id, b.kind, b.quote_text, b.quote_context, b.page_label, b.created_at, b.updated_at
FROM document_bookmarks b
JOIN documents d ON d.id = b.document_id
JOIN authors a ON a.id = d.author_id
JOIN categories c ON c.id = d.category_id
WHERE b.user_id = $1
  AND (
    CASE
      WHEN $4::text IS NOT NULL AND $4::text != '' THEN b.kind = $4::text
      ELSE TRUE
    END
  )
  AND (
    CASE
      WHEN $5::text IS NOT NULL AND $5::text != ''
      THEN (
        d.title ILIKE '%' || $5::text || '%'
        OR COALESCE(d.description, '') ILIKE '%' || $5::text || '%'
        OR a.name ILIKE '%' || $5::text || '%'
        OR c.title ILIKE '%' || $5::text || '%'
        OR COALESCE(b.quote_text, '') ILIKE '%' || $5::text || '%'
        OR COALESCE(b.quote_context, '') ILIKE '%' || $5::text || '%'
        OR EXISTS (
          SELECT 1
          FROM document_tags dt
          JOIN tags t ON t.id = dt.tag_id
          WHERE dt.document_id = d.id
            AND t.title ILIKE '%' || $5::text || '%'
        )
      )
      ELSE TRUE
    END
  )
ORDER BY b.created_at DESC, b.id DESC
LIMIT $2 OFFSET $3
`

type ListBookmarksByUserParams struct {
	UserID uuid.UUID
	Limit  int32
	Offset int32
	Kind   *string
	Search *string
}

// ListBookmarksByUser
//
//	SELECT b.id, b.user_id, b.document_id, b.kind, b.quote_text, b.quote_context, b.page_label, b.created_at, b.updated_at
//	FROM document_bookmarks b
//	JOIN documents d ON d.id = b.document_id
//	JOIN authors a ON a.id = d.author_id
//	JOIN categories c ON c.id = d.category_id
//	WHERE b.user_id = $1
//	  AND (
//	    CASE
//	      WHEN $4::text IS NOT NULL AND $4::text != '' THEN b.kind = $4::text
//	      ELSE TRUE
//	    END
//	  )
//	  AND (
//	    CASE
//	      WHEN $5::text IS NOT NULL AND $5::text != ''
//	      THEN (
//	        d.title ILIKE '%' || $5::text || '%'
//	        OR COALESCE(d.description, '') ILIKE '%' || $5::text || '%'
//	        OR a.name ILIKE '%' || $5::text || '%'
//	        OR c.title ILIKE '%' || $5::text || '%'
//	        OR COALESCE(b.quote_text, '') ILIKE '%' || $5::text || '%'
//	        OR COALESCE(b.quote_context, '') ILIKE '%' || $5::text || '%'
//	        OR EXISTS (
//	          SELECT 1
//	          FROM document_tags dt
//	          JOIN tags t ON t.id = dt.tag_id
//	          WHERE dt.document_id = d.id
//	            AND t.title ILIKE '%' || $5::text || '%'
//	        )
//	      )
//	      ELSE TRUE
//	    END
//	  )
//	ORDER BY b.created_at DESC, b.id DESC
//	LIMIT $2 OFFSET $3
func (q *Queries) ListBookmarksByUser(ctx context.Context, arg ListBookmarksByUserParams) ([]DocumentBookmark, error) {
	rows, err := q.db.Query(ctx, listBookmarksByUser,
		arg.UserID,
		arg.Limit,
		arg.Offset,
		arg.Kind,
		arg.Search,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DocumentBookmark{}
	for rows.Next() {
		var i DocumentBookmark
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.DocumentID,
			&i.Kind,
			&i.QuoteText,
			&i.QuoteContext,
			&i.PageLabel,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
